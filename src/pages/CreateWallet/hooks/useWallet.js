// import walletDomain from '../../../domain/wallet';

export default () => {
  const wallet = null;

  const createWallet = () => {
    // walletDomain.createWallet()
  }

  return {
    wallet,
    createWallet,
  }
}